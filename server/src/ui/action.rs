// =====================================================================
// Copyright (c) 2026 Phiarc Team and St Rangeset
// Licensed under the GNU General Public License Version 3.0 or later
// https://github.com/phi-amateur-radio-community/pa-deploy
// =====================================================================
// Path /server/src/ui/action.rs
// Running action for core

use conf::ConfigServerMap;

/*

impl ScreenData {
    fn create(&mut self) {
        let map = ConfigServerMap::new();
    }

    fn move(&mut self, action: MoveAction) {
        match (action, self.detail_ptr) => {
            (MoveAction::Up, _) => self.limit_move(true),
            (MoveAction::Down, _) => self.limit_move(false),
            (MoveAction::Left, Some(_)) => self.detail_ptr = None,
            (MoveAction::Right, None) => self.detail_ptr = Some(0),
            _ => {}
        }
        trace!(target: "ui/action", explorer_ptr = self.ptr, detail_ptr = self.detail_ptr, "User moved");
    }
}

*/
